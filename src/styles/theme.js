import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    background: {
      default: '#EAE7DC',
      //#FFFAF1 = original
    },
    primary: {
      main: '#653221',
    },
    secondary: {
      main: '#003366',
    },
  },
})

export default theme
